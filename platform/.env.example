# Generate FERNET_KEY: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Generate COOKIE_SECRET: python3 -c "import secrets; print(secrets.token_hex(32))"
PLATFORM_FERNET_KEY=
PLATFORM_COOKIE_SECRET=
